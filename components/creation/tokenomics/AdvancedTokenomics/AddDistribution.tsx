import { Box } from '@mui/material';
import * as React from 'react';
import { ITokenomics } from '../../../../lib/creation/Api';
import { IData } from '../../../../lib/utilities';



const AddDistribution: React.FC<IData<ITokenomics>> = (props) => {
    return <Box sx={{borderRadius: '.3rem', backgroundColor: 'fileInput.outer', display: 'flex', flexWrap: 'wrap'}}>

    </Box>
}

export default AddDistribution;